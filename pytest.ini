[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
markers =
    unit: fast unit tests (no db, no network)
    integration: tests requiring temp db
    smoke: tests requiring live MCP server
